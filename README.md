# LocationUser
Get users' location based on ip and add them to a database table

Program in progress to get the users location and store them in a database table based on their ip with simple rest services.

